# NQ Bot Project 🤖

הפרויקט הזה מסונכרן לענן באמצעות GitHub.

## איך לעבוד ממחשב אחר?

1.  **התקנת כלים**: וודא שמותקנים אצלך [Git](https://git-scm.com/) ו-[VS Code](https://code.visualstudio.com/).
2.  **הורדת הפרויקט**:
    פתח טרמינל (Terminal) בתיקייה שבה תרצה לשמור את הפרויקט והרץ:
    ```bash
    git clone https://github.com/kagan1405-lgtm/nq-bot.git
    ```
3.  **פתיחה**: פתח את התיקייה שנוצרה ב-VS Code.

---

## איך לסנכרן שינויים? (Work Flow)

### לפני תחילת עבודה
תמיד תמשוך את הגרסה העדכנית מהענן כדי לא לדרוס עבודה של עצמך:
```bash
git pull
```

### בסיום עבודה
כדי לשמור את השינויים שלך ולעדכן את הענן:
1.  **הוספת קבצים**:
    ```bash
    git add .
    ```
2.  **שמירת גרסה (Commit)**:
    ```bash
    git commit -m "תיאור קצר של מה שעשיתי"
    ```
3.  **שליחה לענן (Push)**:
    ```bash
    git push
    ```
